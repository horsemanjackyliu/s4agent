"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getSchemaType = getSchemaType;
const entity_1 = require("../../entity");
const navigation_properties_1 = require("./navigation-properties");
/**
 * @internal
 */
function getSchemaType(entity, service) {
    return `{
    ${[
        ...propertyTypes(entity),
        ...(0, navigation_properties_1.navigationPropertyTypes)(entity, service),
        allFieldsType(entity.className)
    ].join(',\n')}
  }`;
}
function propertyTypes(entity) {
    return entity.properties.map(prop => propertyType(entity.className, prop));
}
function propertyType(className, prop) {
    return [
        `${prop.staticPropertyName}: ${(0, entity_1.createPropertyFieldType)(className, prop)}`
    ].join('\n');
}
function allFieldsType(className) {
    return [`ALL_FIELDS: AllFields<${className}<DeSerializers>>`].join('\n');
}
//# sourceMappingURL=schema-type.js.map