import type { PathLike } from 'fs';
import type { SwaggerMetadata, SwaggerPath } from './swagger-types';
/**
 * @internal
 */
export declare function readSwaggerFile(swaggerPath: PathLike): SwaggerMetadata;
/**
 * @internal
 */
export declare function getSwaggerDefinitionForOperation(originalName: string, httpMethod: string, swaggerMetadata: SwaggerMetadata | undefined): SwaggerPath | undefined;
